import React from "react";
import { colors } from "../../utls/colors";
import { dateTimeForemt } from "../../helper/dateTIme";

function Home() {
  // dateTimeForemt(new Date())
  return (
    <div>
      <div style={{ color: colors.primary }}></div>
    </div>
  );
}

export default Home;
